package pagerankcounter;

/*
 * Really crude/hackish method of including the counter enum.
 * Either java or eclipse wasn't able to process the import from default package.
 */
public class PageRankCounterClass {
	public enum PageRankCounter{
		RESIDUAL;
	}
}
